import React from 'react'

export const Carting =(Con,w)=> {

    return (
    
        <section>

            <div>

                <p key={w}>{Con.name}</p>
                
            </div>

        </section>

     
    )
}